[pytest]
markers =
    integration: marks tests that require live external services (Alpaca sandbox, real DB).
                 Run with: pytest -m integration
                 Skip with: pytest -m "not integration"

# Default: exclude integration tests so CI passes without credentials
addopts = -m "not integration"
